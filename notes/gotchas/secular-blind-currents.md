---
title: secular-blind-currents
---

Global (secular) steady states commute with H, so every bond current
i<[V_b, h_i]> is identically zero and bath heat lands in the interaction
energy. `heat_flow_map` flags it (`extras["secular_blind"]`). Resolve internal
transport with local baths (weak coupling) instead.
